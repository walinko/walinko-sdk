# Changelog

This file tracks repo-wide changes (OpenAPI spec, shared docs, CI). Per-SDK
changelogs live at `sdks/<lang>/CHANGELOG.md`.

## [Unreleased]

### Added
- Monorepo scaffolding with Ruby and PHP SDK skeletons.
- `docs/openapi.yaml` describing the public messaging API (`v1`).
- `docs/error-codes.md` canonical error code reference.
